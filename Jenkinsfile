pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building auth-service'
                // Add your build commands for the auth service
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying auth-service to k8s'
                // Add your deployment commands for auth service
            }
        }
    }
}
