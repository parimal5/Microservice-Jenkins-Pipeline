pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building order-service'
                // Add your build commands for the order service
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying order-service'
                // Add your deployment commands for order service
            }
        }
    }
}

